<h2><%= link_to "#{@post.title}", @post.link %> <%= @post.points %></h2>

<p><%= link_to "Back to Posts", posts_path %> | <%= link_to "Edit", edit_post_path(@post.id) %> | <%= link_to "Destroy", post_path(@post.id), method: :delete, confirm: 'Are you sure?' %></p>
